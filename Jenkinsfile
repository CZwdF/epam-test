node {
  stage('Example') {
    steps {
        script { 
            if (env.BRANCH_NAME ='master') {
                echo 'This is  master'
            } else {
                echo 'things and stuff'
            }
        }
    }
}
